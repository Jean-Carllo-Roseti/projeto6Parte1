import styled from 'styled-components'
import { cores } from '../../../styles'

export const CardSide = styled.div`
  width: 344px;
  height: 100px;
  background-color: ${cores.trigo};
  color: ${cores.salmao};
  display: flex;
  padding: 8px;
  margin-bottom: 16px;
`
export const SideImg = styled.img`
  width: 80px;
  height: 80px;
  margin-right: 8px;
`

export const DescriptionSide = styled.p`
  color: ${cores.salmao};
`
